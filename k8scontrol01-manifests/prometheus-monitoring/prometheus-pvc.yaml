apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-data-pvc
  namespace: monitoring
  labels:
    app: prometheus-storage
spec:
 accessModes:
   - ReadWriteOnce
 resources:
   requests:
      storage: 4Gi